#ifndef __ZFS_EXPORT_H__
#define __ZFS_EXPORT_H__

/*
#include "ZfsBasic.h"


class ZFS_API ZFExportParam
{
public:
    std::string out;    // out filename
    int fromx;
    int tox;
    int fromy;
    int toy;
    int subx;
    int suby;
    bool bRf;
    bool bHeader;
    double rgfrom;      // min range
    double rgto;        // max range
    int minrf;          // minumum intensity
    std::string format;
    std::string param;  // additional params

    ZFExportParam();
};

ZFS_API int Export( ZFScan*, ZFExportParam& );
ZFS_API int Export( ZFScan*, const char* );

*/

#endif //__ZFS_EXPORT_H__
