#ifndef FILETERDLL_TOOLS_H
#define FILETERDLL_TOOLS_H

#include <string>

#include "filterapi.h"

/*
class ZFSHeader;

bool AddFilterFlag( const char* out, const char* str, const char* inf=NULL );
bool CheckFilter( ZFSHeader& h, const char* filter );
void FILTERDLL_API ChangeNameIlar( const std::string& add, std::string& file );
*/

#endif // FILETERDLL_TOOLS_H
